/**
 * Sound file map. Leave paths null to keep audio silent.
 * Assign URLs later, then call audio.load(SOUND_FILES) and set audio.enabled = true.
 */
export const SOUND_FILES = {
  catch: null,
  miss: null,
  lifeLost: null,
  levelUp: null,
  gameOver: null,
  music: null,
};
